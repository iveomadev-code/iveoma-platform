import PrivacyPageClient from './PrivacyPageClient';

export const metadata = {
  title: 'Privacy Protocol | Iveoma Development Network',
  description: 'Our data governance and privacy protocols ensure transparency and trust in our institutional operations.',
};

export default function PrivacyPage() {
  return <PrivacyPageClient />;
}
