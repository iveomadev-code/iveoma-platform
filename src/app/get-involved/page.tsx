import GetInvolvedClient from './GetInvolvedClient';

export const metadata = {
  title: 'Get Involved | Iveoma Development Network',
  description: 'Partner with Iveoma Development Network to drive systemic, high-fidelity impact in community development.',
};

export default function GetInvolvedPage() {
  return <GetInvolvedClient />;
}
