import EvidencePageClient from './EvidencePageClient';

export const metadata = {
  title: 'Impact Evidence | Iveoma Development Network',
  description: 'Detailed impact statistics, timeline, and category breakdowns of institutional interventions.',
};

export default function EvidencePage() {
  return <EvidencePageClient />;
}
